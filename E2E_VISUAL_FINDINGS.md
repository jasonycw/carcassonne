
## Passing evidence QA

The passing River-phase screenshot shows a two-player scoreboard, the active player turn indicator, the visible `River phase` label, the River source already on the board, and a River tile in the active-tile preview. The passing Tower-action screenshot shows the same two-player scoreboard, an expanded River board, clickable highlighted placement squares, and the Tower-enabled turn flow; the optional Tower action HUD is represented by the gameplay capture and video even when the final screenshot is after the action is skipped.

The evidence test passed after the official no-legal-placement fallback and CcII edge correction. The saved video is `.playwright/river-tower-multiplayer-Th-999f8-oses-official-Tower-actions-chromium/video.webm`; screenshots are `river-phase-start.png` and `tower-actions.png` in the same directory.
