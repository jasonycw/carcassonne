Base on testing with latest version on `https://jasonycw.github.io/carcassonne/`, there are at list and not limited to the following issues that cause the game not fully working.
- When I zoom out of the map and then change location for the tile, the tile is being snapped back to 100% size and then zoom to the destination which is incorrect. The original game behavior at commit  `962f33eed833cb2cb845e40bb5d7186e66dc6d2d` is that the animation of the temporary tile is anchor together with the map, so once I click on a tile temporary location and start shaking the map, the animation will still move and zoom base on the map following the same shake and zoom.
- During placing tile state, when click on outside non tile place that is not available to place a tile, the temporary tile should be back to the top right WITH ANIMATION
- After placing a temporary tile, when click non meeple outline spaces of the tile, it should be back to temporary tile state with one click, remove the meeple outline rotate and show the rotate indicator immediately again. Right now it needs 2 click to make it rotate.
- tower extension is disabled in the original version at commit  `962f33eed833cb2cb845e40bb5d7186e66dc6d2d`? If it's true, then should remove it. If it deem to be kept, double confirm the logic by checking online how the expansion work.
- For joiner of the game, when refresh the page, should join back to the host, right now it's connection lost and just stuck at the lobby. It should be able to join back in to the same game,
- For the host, if the remote joiner lose connection, it should have an indicator next to their tag showing connection lost should allow them to connect back in, it should show they are back.
